---
- name: Check if /dev/sdc is already a PV in vgdata
  command: vgs --noheadings -o pv_name vgdata
  register: vg_pvs
  changed_when: false

- name: Extend VG only if /dev/sdc is NOT already included
  command: vgextend vgdata /dev/sdc
  when: "'/dev/sdc' not in vg_pvs.stdout"
